export const repoExists = (repoName: string) => {
  
};

export const getRepo = (username: string, repo: string) => {
  const endpoint = `https://api.github.com/repos/${username}/${name}`
}

export const cloneRepo = () => {

};

export const extractRepo = () => {

};

export const gitInit = () => {

};